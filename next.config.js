/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable experimental features if needed
  experimental: {},

  // Environment variables that are safe to expose to the browser
  // (none needed - all sensitive keys stay server-side)
  env: {},
};

module.exports = nextConfig;
